# Pre-commit hooks for dbt project
# See https://pre-commit.com for more information

repos:
  # General file formatting hooks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
        args: ['--unsafe']  # Allow custom YAML tags used in dbt
      - id: check-added-large-files
        args: ['--maxkb=1000']
      - id: check-case-conflict
      - id: check-merge-conflict
      - id: debug-statements
      - id: mixed-line-ending

  # # SQL linting and formatting with sqlfluff
  # - repo: https://github.com/sqlfluff/sqlfluff
  #   rev: 3.0.7
  #   hooks:
  #     - id: sqlfluff-lint
  #       name: sqlfluff-lint
  #       args: ['--dialect', 'snowflake', '--disable-progress-bar']
  #       additional_dependencies: ['dbt-snowflake', 'sqlfluff-templater-dbt']
  #     - id: sqlfluff-fix
  #       name: sqlfluff-fix
  #       args: ['--dialect', 'snowflake', '--disable-progress-bar']
  #       additional_dependencies: ['dbt-snowflake', 'sqlfluff-templater-dbt']
